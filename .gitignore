# Minimal .gitignore - Only Critical Build Exclusions
# This approach hides minimal information while protecting essentials

# Build outputs (critical to exclude)
pogocache
*.o
*.a
gitinfo.h

# Downloaded dependencies (too large for repo)
deps/openssl/
deps/liburing/
deps/*.tar.gz

# Generated files
*.dSYM/
*.out
a.out